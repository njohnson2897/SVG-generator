const Circle = require('../lib/circles.js')

describe('Circle', () => {
    it('should render      ', () => {
        const circle = new Circle()

        
        expect(circle.render()).toEqual()
    })
})