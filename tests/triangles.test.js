const Triangle = require('../lib/triangles.js')

describe('Triangle', () => {
    it('should render      ', () => {
        const triangle = new Triangle()

        
        expect(triangle.render()).toEqual()
    })
})